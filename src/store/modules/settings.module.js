
const initialState = () => ({
});

const state = initialState;

const getters = {
};

const mutations = {
};

const actions = {
};

export default {
  namespaced: true,
  getters,
  actions,
  mutations,
  state,
};
